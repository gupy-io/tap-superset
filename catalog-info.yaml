# Esse arquivo é relativo ao catálogo de projetos da gupy
# documentacao completa https://backstage.tools.internal.gupy.io/docs/default/Component/projects-catalog
# O mínimo que cada repositório deve incluir no catálogo de projetos é um componente do System Model do backstage
# consulte detalhes na documentação do catalogo de projeto ou em https://backstage.io/docs/features/software-catalog/system-model/
# após preenchimento , apagar os comentários
#
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: tap-superset                              # Alterar : Nome do projeto ou repositório
  description: Tap for superset entities extraction                              # Alterar : Descrição do projeto
  annotations:
    github.com/project-slug: gupy-io/tap-superset # Alterar : Nome do repositório
    backstage.io/techdocs-ref: dir:.
  tags:
      - python                                            # Alterar : tags de principais frameworks,linguagem, runtime (k8s,lambda,ecs,cloud function, cloud run)
      - meltano                                      # Alterar : tags de principais frameworks,linguagem, runtime (k8s,lambda,ecs,cloud function, cloud run)
spec:
  type: library                                         # Alterar : 'service', 'website' ou 'library'
  owner: plataforma-de-dados                            # Confome nome do time no GitHub ('qualificacao', 'vagas', 'candidates', por exemplo)
  lifecycle: production                                 # Alterar : experimental, production, deprecated
